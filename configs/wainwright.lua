Config.wainwright = {
    -- Restrict leveling and shops to configured wainwright jobs.
    wainwrightOnly = false,

    wainwrightJobs = {
        wainwright = {
            minimumGrade = 0,       -- Lowest job grade allowed.
        },
    }
}